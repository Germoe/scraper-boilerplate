from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a boilerplate for the cookie cutter data-science template that adds a rotating IP scraper based on Python',
    author='Sebastian Engels',
    license='MIT',
)
